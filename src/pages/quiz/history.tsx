/**
 * The 'QuizHistory' route should show students every quiz they have completed.
 **/
import { NextPage } from 'next'

const QuizHistory: NextPage = () => {
    return <p>List of all quizzes you have completed</p>
}

export default QuizHistory